// @umbral/engine — el CEREBRO. TS puro: sin DOM, sin pixi, sin react (INV-3).
// reduce(state, action) -> { state, events } llega en el Bloque 2/3.
import { UMBRAL_SCAFFOLD } from '@umbral/shared';

export const ENGINE_SCAFFOLD = `umbral-engine@0 (sobre ${UMBRAL_SCAFFOLD})`;
