#!/usr/bin/env tsx
// @umbral/sim — herramientas de simulacion/balanceo (CLI que juega miles de runs).
// Bloque 0: CLI vacio. La simulacion real (miles de runs, win-rate por Velo) llega en el Bloque 19.

function main(): void {
  console.log('umbral-sim @0 — CLI de balanceo (vacio). La simulacion llega en el Bloque 19.');
}

main();
