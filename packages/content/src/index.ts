// @umbral/content — TODO el contenido como data tipada (reliquias, jefes, eventos...).
// Sin logica de engine: solo data + efectos declarativos (DSL). Llega desde el Bloque 5.
export const CONTENT_SCAFFOLD = 'umbral-content@0';
