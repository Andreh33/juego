# UMBRAL — variables de entorno (copia a apps/web/.env.local y rellena).
# Turso / libsql (solo servidor; NUNCA con prefijo NEXT_PUBLIC).
TURSO_DATABASE_URL=libsql://TU-BASE.turso.io
TURSO_AUTH_TOKEN=TU_TOKEN_RW

# Version de contenido/balanceo activa (debe coincidir cliente/servidor, INV-7).
RULESET_VERSION=1
